import React, { useRef, useState } from "react";

const ProductCard = ({ }) => {


    return (
        <div className="product-card">
            
        </div>
    );
};

export default ProductCard;
